PLSQL Triggers (continued)

we have 
	before trigger
	after trigger
	instead of trigger


FOR EACH ROW		trigger	can access magic tables 	(:new, :old)
vs
STATEMENT 		trigger


INSTEAD OF TRIGGER CAN FIRE ONLY FOR "VIEW"s


---------------------------
ERRORs
---------

we can raise application error whenever we find something is not ok.

raise_application_error(-20000,"this is not ok");	

pragma exception_init		is used to associate a name of exception to a number

EXCEPTION block is used to handle the exception.


Task:
------
1) try instead of trigger
2) raise error if some condition is true
3) associate names to error codes using pragma exception init
4) handle exceptions using EXCEPTION block

==========================================
Java
-----
Sun microsystems developed a new language to address the issues found in c++,
and they used most of the syntax of c, c++ and added new features also.
in 1995

Oracle corporation adopted Sun microsystems 

java 1.1		to 1.4			Java 2

major change happened 	in 		java 5		(1.5 = 5)
major:

	Java 5		for-each		generics		Scanner
	Java 8		
	Java 11
	Java 17
	Java 21

these are called as LTS versions of java

java is platform independent

Linux	/ Android
MacOs
Windows

how it is possible?

java programs are compiled to "byte" code which is not specific to any platform.

at runtime, it is further compiled again into machine specific code 

who does this compilation?
	at run time, JRE (Java runtime environment)
		uses JIT (Just In Time) compiler to convert byte code to final machine code


JVM	is a process, that contains threads like
	Garbage Collector, Class Loader, JIT Compilers

JRE 	Java Runtime Environment

we can download jre to run any java code.

JDK vs JRE
	JDK is complete, we developer need jdk to 
		compile
		execute

	JRE is just runtime to
		execute 



Lab setup:
------------
jdk is already installed in your computer

Microsoft Windows [Version 10.0.19045.6093]
(c) Microsoft Corporation. All rights reserved.

C:\Users\rjaga>java --version
java 21.0.2 2024-01-16 LTS
Java(TM) SE Runtime Environment (build 21.0.2+13-LTS-58)
Java HotSpot(TM) 64-Bit Server VM (build 21.0.2+13-LTS-58, mixed mode, sharing)



we can use IDEs like
	Eclipse
	Spring Tool Suite	(is also an eclipse only)
	Visual Studio code
	IntelliJ
	

 Task:
------
create a hello world program in java and run in cmd prompt.
write code in notepad

public class Hello{
	public static void main(String []jags){
		System.out.println("Hello Jag");
	}
}

=========================
Java primitive data types:-
-----------------------------
boolean		1 bit		false, true

byte		1 byte		-128 to 127
short		2 bytes		-32768 to 32767
int		4 bytes		-2,14,74,83,647 to 2,14,74,83,646
long (8 bytes)	-9,223,372,036,854,775,808	9,223,372,036,854,775,807

float		4 bytes
double		8 bytes

char		2 bytes



Programming constructs:

if
if else
if else if else
if else if else if else

switch case

while loop

do while loop

for loop

for each loop




conditions need operators

+
-
*
/
%

++
--

=
!=
+=
-=
*=
/=
%=

>
<
>=
<=

!
&&
||

>>
<<


Activity:
understand the use of the above operators and programming constructs.



Handson for if:
-----------------

Dice Game I

Each player will be allowed to throw  a dice 2 times. The points for each player will be calculated as follows :
If the values thrown are different, the points scored is equal to the sum of the 2 values.
If the values thrown are the same, the points scored is equal to double the sum of the 2 values.

Write a program to calculate the points scored by a player.

Input and Output Format:

Input consists of 2 integers. The valid range of inputs is from 0 to 6. [ Both 0 and 6 inclusive]
Output the points scored. In case of invalid inputs, print “Invalid Input”.

Refer sample input and output for formatting specificaions.

[All text in bold corresponds to input and the rest corresponds to output.]
Sample Input and Output 1:

Enter Value 1:
5
Enter Value 2:
6
The points scored is 11

Sample Input and Output 2:

Enter Value 1:
5
Enter Value 2:
5
The points scored is  20

Sample Input and Output 3:

Enter Value 1:
8
Enter Value 2:
6
Invalid Input 








To get input from the user,
	we use Scanner class (java.util package)


Scanner sc=new Scanner(System.in);			//System.in refers to input 							device





import java.util.Scanner;

public class App1 {

	public static void main(String[] args) {
		Scanner sc=new Scanner(System.in);
		System.out.println("Enter Value 1:");
		int value1=sc.nextInt();
		System.out.println("Enter Value 2:");
		int value2=sc.nextInt();
		
		if(value1<0 || value1>6 || value2<0 || value2>6) {
			System.out.println("Invalid Input");
//			System.exit(0);
			return;		//returns main method means, end of program execution
		}
		//if above if is not true, then only the below lines are executed.
		int sum=value1+value2;
		/*
		 *  int points=sum; if(value1==value2) { // points=sum*2;
		 * this is same as below points*=2; }
		 * 
		 * System.out.printf("The points scored is %d",points);
		 */
		
		System.out.printf("The points scored is %d",(value1==value2)?2*sum:sum);
	}

}







Dice Game II
Each player will be allowed to throw a dice 2 times. The points for each player will be calculated as follows :
The points scored is the absolute difference between 8 and the sum of the 2 values, provided the sum of the 2 values is less than 8.
In all other cases, the point  scored is double the absolute difference between 8 and the sum of the 2 values.

Write a program to calculate the points scored by a player.

Input and Output Format:
Input consists of 2 integers. The valid range of inputs is from 0 to 6.
Output the points scored. In case of invalid inputs, print “Invalid Input”.

Refer sample input and output for formatting specificaions.

[All text in bold corresponds to input and the rest corresponds to output.]
Sample Input and Output 1:

Enter Value 1:
5
Enter Value 2:
6
The points scored is 6

Sample Input and Output 2:

Enter Value 1:
3
Enter Value 2:
2
The points scored is  3

Sample Input and Output 3:

Enter Value 1:
8
Enter Value 2:
6
Invalid Input 






Solution for dice2:
-



import java.util.Scanner;

public class Dice2 {
	public static void main(String[] args) {
		Scanner sc=new Scanner(System.in);
		System.out.println("Enter Value 1:");
		int value1=sc.nextInt();
		System.out.println("Enter Value 2:");
		int value2=sc.nextInt();
		
		if(value1<0 || value1>6 || value2<0 ||value2>6) {
			System.out.println("Invalid Input");
			return;
		}
		
		int sum=value1+value2;
		int points=Math.abs(8-sum);
		if(sum>=8) {
			points*=2;
		}
		
		System.out.printf("The points scored is %d",points);
	}
}



int x=20;
x++;			//increment alone
System.out.println(x);		//what is x?		x is 21



-------------------


int x=20;
int y=x++;		//x is assigned to y and then x increments by 1
System.out.println(y);	//what is y?		y is 20



--------------------

int x=20;
int y=++x;		//x increments by 1 and then x is assigned to y
System.out.println(y);	//what is y?		y is 21



whenever an expression uses multiple operators, then it will use BODMAS rule

3/2*5		=	

in java, int types always result int		no decimal places


int i=3;
int j=2;

int k=i/j;			//is 1			not 1.5
====================but==========

double i=3;
double j=2;
double k=3/2;				it is 1.5


---------------------------


Memory structures in java.

int i=20;			int is primitive type.


	=============================================
Arrays
--------
Arrays are used to store multiple values or references of same (data)type.

arrays can be primitive or reference type.

array name is the reference to 1st element of the array.

index is used to refer to the relatively how many locations, we need to shift to find that element.

For example:
arr is the name of array

arr refers to 1st element				0 is the index
you have move 1 more location for second element	1 is the index 



arrays have advantage:
	1) lot of values referred by single name
	2) we can find any element directly using index
	

arrays have disadvantage:
	1) fixed length		we should know the size of array in prior

		can we change the length of array?
			Mithun says yes. How mithun?

syntax for arrays.

1) declare the array
	int []arr;			//we have declared the array

	Employee [][]employees;		//2 dimensional

2) sizing the array
	arr=new int[10];
	employees=new Employee[10][];	//10 rows and each row can be of diff




int []arr=new int[10];

arr[0]	is first element of the array. it is an int variable only

arr[9]	is the last element of the array




int arr[]=new int[10];		//correct

int []arr= {81,29,13,41,25,63,47};




How will you sort an array?
	Array related operations are provided by Arrays class

import java.util.Arrays;





package arrays;

import java.util.Arrays;

public class App {

	public static void main(String[] args) {
		int []arr= {81,29,13,41,25,63,47};
		
		Arrays.sort(arr);
		
		System.out.println(Arrays.toString(arr));
	}

}



package arrays;

import java.util.Arrays;

public class App {

	public static void main(String[] args) {
		Integer []arr= {81,29,13,41,25,63,47};
		
		Arrays.sort(arr);

//		for(int i=arr.length-1;i>=0;i--) {
//			System.out.println(arr[i]);
//		}
		
		for(Integer x:arr) {
			System.out.println(x);
		}
	}

}


Task:
-------
Ask the user to enter the number of countries.	x

then accept x inputs from the user and store each country in an array. 	arr

sort the arr by alphabetical order.

display all the countries found in arr.


sample input:
5
India
Australia
New zealand
Sri lanka
Japan


package arrays;

import java.util.Scanner;

public class Countries {

	public static void main(String[] args) {
		Scanner sc=new Scanner(System.in);
		int noOfCountries=sc.nextInt();
		String []arr=new String[noOfCountries];
		for(int i=0;i<noOfCountries;i++) {
			arr[i]=sc.nextLine();
			if(arr[i].equals("")) {
				arr[i]=sc.nextLine();
			}
		}
		
		//display all countries
		
		for(String x:arr) {
			System.out.println(x);
		}
	}

}





We should discuss about scanner next methods


You are using scanner methods like
	nextInt
	next
	nextLine

you need to understand the delimiters used by scanner methods.

scanner method capture the input until a delimiter is found.

what are the delimiters
	space		' '
	tab		\t	
	new line		\n


when we give input as
1 raja 20

nextLine() is the only method of Scanner that will capture inputs that contains space and tab.	this nextLine captures input until a new line is found.	<enter> key is 


all other methods
	nextInt
	nextFloat
	nextDouble
	nextLong

etc
will capture until a space or tab or new line is found.


so, if you use nextLine() after any other next methods, your nextLine will capture the empty input.

Demo:
--------

import java.util.Scanner;

public class App {

	public static void main(String[] args) {
		Scanner sc=new Scanner(System.in);
		Scanner sc2=new Scanner(System.in);
		System.out.print("Enter your employee id:");
		int employeeId=sc.nextInt();
		
		System.out.print("Enter your name:");
		String name=sc2.nextLine();
		name=sc.nextLine();
		System.out.print("Enter your score:");
		float score=sc.nextFloat();
		
		System.out.printf("Employee id:%d\nEmployee Name:%s\nScore:%f\n",employeeId,name,score);
		
	}

}






















import java.util.Scanner;

public class App {

	public static void main(String[] args) {
		Scanner sc=new Scanner(System.in);
		System.out.print("Enter your employee id:");
		int employeeId=sc.nextInt();
		
		System.out.print("Enter your name:");
		String name=sc.nextLine();
		if(name.equals("")) {
			name=sc.nextLine();
		}
		System.out.print("Enter your score:");
		float score=sc.nextFloat();
		
		System.out.printf("Employee id:%d\nEmployee Name:%s\nScore:%f\n",employeeId,name,score);
		
	}

}




classes
---------
a class wraps data and logic together as single type

how to create a class?
	class <<classname>>





primitives can never be null.		There are default values for primitives


private		members are only accessible within the class
default		members are only accessible within the package
protected		members are only accessible within the package and also by sub 			classes of any package
public		members are only accessible from anywhere







public class Product {
	private Integer id;
	String name;				//package scope
	protected String category;	//same package + sub classes of any package
	public Double price;
	
	public void display() {
		//here, id, name, category, price all are visible
	}
}


POJO		Plain Old Java Object

	does not inherit any super class
	does not implement any interface





int arr[]=new int[10];			//complete
Product parr[]=new Product[10];		//not complete

parr[0]=new Product();			//you have to do that




int x=0;			//complete

Product p=null;		//not complete
p=new Product();		//complete





https://github.com/jagasia/empower2025/blob/jag/Java/Java_Handson_1.docx


to retrieve the digits one by one from a number:
----------------------------------
	modulo by 10 will give last digit.
	then remove that last digit by / by 10
	then continue to get last digit every time.




To convert an int to String
------------------------------
	String.valueOf(int)
	int i=545;
	String str=String.valueOf(i);

	(or)

	int i=545;
	String str=i+"";


String members can be accessed using:
		charAt(index)

	str.chatAt(0)		is first character
	str.charAt(1)		is second character


StringBuilder or StringBuffer
	they have reverse() method that reverses a String

String has toCharArray() method
	char []arr=str.toCharArray();

then you can loop index i to arr.length-1
and access each character using index in for loop



