String
-------
like already we have discussed, String is peculiar

String str1="hello";		//hello is not a value. it is an object	constant object

"hello" is a String literal

String str1="hello";		
String str2="hello";

str1 and str2 refers to same memory locations. 
so		str1==str2		true

str1.equals(str2)				true


equals method is code in such a way, the literals are compared. 

------------------
String class
--------------
is immutable.
means, cannot update.



String str1="equals method is code in such a way, the literals are compared. ";
str1="equals method is code on such a way, the literals are compared. ";


String is not editable.

str1="hello";
str1="hi";		


matches() method		returns true, if the string matches a regex pattern

Activity:
String methods and properties

length()
substring()
charAt()
matches()
toUpperCase()
toLowerCase()
indexOf()



str1=str1.toUpperCase();

StringBuilder/ StringBuffer

Difference between them 
	StringBuffer is synchronized. So it is thread-safe. But slow, because it locks 

StringBuilder is fast. But not thread safe



	

Demo:

Accept 2 string inputs from user and check if they are "anagrams"

eat
ate


team
meat


abcd
bcda

solution1:
------------
if both string lengths are not same, then result is false
sort the characters in alphabetical order of both string
now, if they are same then they are anagrams



import java.util.Arrays;
import java.util.Scanner;

public class App {

	public static void main(String[] args) {
		Scanner sc=new Scanner(System.in);
		String str1=sc.next();
		String str2=sc.next();
		
		char[] carr1 = str1.toCharArray();
		char[] carr2 = str2.toCharArray();
		
		Arrays.sort(carr1);
		Arrays.sort(carr2);
		
		//convert char array into string
		
		String x=new String(carr1);
		String y=new String(carr2);
		
		if(x.equals(y)) {
			System.out.println("Anagrams");
		}else {
			System.out.println("Not anagrams");
		}
		
	}

}





check if a string is palindrome?

reverse method is there in StringBuilder

-------------------------------------
OOPS	Object Oriented Programming System.

class		is a blue print for creating objects
		properties and methods

object		is an instance of class
		all objects share the same behavior but different identity

Inheritance
Encapsulation
Abstraction
Polymorphism




Abstraction
-------------
public methods that are allowed 
other members of a class are hidden

Encapsulation
---------------
enclose all data and functions inside a type (class/interface)


Inheritance
-------------
Parent - Child

allows us to create classes from existing class.
create interfaces from existing interface.

we do not need to create a class from the scratch. Rather, we can dervice it from another class. So that we get benefits of parent class and add features in child class.


super class
	a class becomes super class, when any other class, inherits this class.
	this is parent class.
	this is base class

sub class
	a class that is derived FROM another class, becomes a sub class for that another
	child
	derived class

Java does not allow multiple inheritance.
	a class can inherit only 1 super class

	multiple inheritance means, a class inherits more than 1 super class (in c++)

Object
	is the super class for any class that do not inherit a super class

class Employee
{}
	the above Employee class does not inherit any super class, so	
		Object is the super class for Employee


Object class has some methods
	toString()
	hashCode()
	equals()
	

multiple inheritance	NOT ALLOWED
muliple sub classes		ALLOWED
multi level 		ALLOWED
hybrid			NOT ALLOWED

how to inherit a class
	use 	"extends" keyword


Polymorphism:
-----------------
is the ability to behave differently in different situation

sum(2,3)			=	5

sum("raj","kumar")		=	rajkumar


when i passed numeric arguments it added (sum)
when i passed string arguments it concatenated (append)

this is different behavior

static polymorphism
-----------------------
during compilation itself 2 versions are available means, static polymorphism

method overloading
	is an example for static polymorphism





class Maths
{
	public int sum(int i, int j) {
		return i+j;
	}
	
	public String sum(String s1, String s2) {
		return s1+s2;
	}
}


the above example for method overloading, there is difference in signature.

signature:
	argument/parameters
	number of parameters
	type of parameters
	order or type of parameters



return type is not signature.
	return type may be same or difference that does not decide polymorphism



public int sum(int i, int j)		
public int sum(int j, int i)			//NOT ALLOWED. both are same



public float sum(int i, float f)
public int sum(int i, float f)			//NOT ALLOWED. return type only is diff


public int sum(int i, int j, int k)
public int sum(int i, int j)			//ALLOWED. difference in no of arg





Demo:
--------
Shape class		(super class)

Triangle class		(sub class)
Rectange class		(sub class)

Triangle:
	calculateArea()
		should print	"1/2*b*h"

Rectange:
	calculateArea()
		should print	"l*b"

App.java
	ask user what they want to draw?
accordingly 
	call the calculateArea() method of either triangle or rectangle




A super class have abstract method:
	sub class is forced to override the method
	if sub class does not override that method, then a copy of abstract method is
		present in sub class. so sub class must be declared as abstract
			or override that abstract method to hide it


Abstract method can be present only in abstract class
A class can have 0 or more abstract methods and can also have concrete methods




Employee mithun=new Employee();
mithun=new ProjectLeader();
mithun=new ProjectManager();




Interface
----------
all inside the interface, all PUBLIC by default. Even if you don't mention also
before java 8, interfaces can have methods that are abstract only.

from java 8 onwards, default and static methods with body are allowed.

Can interface contain variables?
	NO. 		but methods inside interface can have variables.
	ex:		static methods, default methods of interface can have variable, for loops, while, etc

Only constants are allowed
	
all data members of an interface are 
	public static final		//even if you do not mention also







multiple inheritance benefits achieved using interface
	YES
but how?
let me tell you.


in multiple inheritance, we will be able to call 
	method of 2 class from 1 object


A		B

	C





public interface Person{
	void speak();
	int AGE=20;		//allowed
}


public class Student implements Person{
	public void speak()	{
		System.out.println("Student speaks");
	}
}

public class App{
	public static void main(String []args)
	{
		Student raja=new Student();
		raja.speak();
	}
}




Overriding rules:
	overriding method (sub class) cannot reduce the visibility
	signature must be same
	return type must be same
	method name must be same.
	
---------------------------
We will discuss about interfaces in java 8+ after handson.

Hands on tasks:
--------------------

https://github.com/jagasia/empower2025/blob/jag/Java/Java_Handson_1.docx


How to convert String into int?
	String str="120";
	int i=Integer.parseInt(str);		//returns int

Integer.valueOf(str)		returns Integer

Float.parseFloat(str)	returns float



Integer.MAX_VALUE		returns the largest integer value


Wrapper class:
for each primitive data type, corresponding wrapper class is there in java, to provide the support for operations related to those primitive type.

boolean	Boolean
byte	Byte
short	Short
int	Integer
long	Long
float	Float
double	Double
char	Character



Character.isDigit()			//digits are single digit numbers
Character.isLetter()
Character.isNumber()


--------------------------------------------------------
Java 8 interfaces
--------------------
from java 8 onwards, interfaces can have
	method with body	when the method is 
		static method (or)
		default method



static
final

static:
	lets understand static

static variables
static methods
static blocks

static variables:
------------------
	what is a static variable?
		in java, static variables must be at class level. Not inside a 			method.	(in c++, static variables can be inside 				method)


static is opposite to instance	(instance means object)



public class Employee {
	private Integer id;
	private String name;
	private String department;
	private static String companyName;	//why? this is static
	
	public Employee() {
		//here we can write initialization code that is
		//automatically called during object creation
	}

	public Integer getId() {
		return id;
	}

	public void setId(Integer id) {
		this.id = id;
	}

	public String getName() {
		return name;
	}

	public void setName(String name) {
		this.name = name;
	}

	public String getDepartment() {
		return department;
	}

	public void setDepartment(String department) {
		this.department = department;
	}

	public String getCompanyName() {
		return companyName;
	}

	public void setCompanyName(String companyName) {
		this.companyName = companyName;
	}
	
	
}




static members of a class are shared by all object of the class. ie., it is one for the class. not separate for every object.


public class Employee {
	private Integer id;
	private String name;
	private String department;
	private static String companyName;	//why? this is static

}


if i create 2 objects of Employee


Employee raja=new Employee();
Employee john=new Employee();

raja can have separate id, name, department
john can have separate id, name, department

but both have same companyName, because, it is static




static method:
-----------------
	a static method is called using classname but not object name



final
------
	final data member
		is CONSTANT
		cannot be changed

	ex:
		Integer.MAX_VALUE

	final method
		cannot be overridden
	final class
		cannot be inherited




static
final
transient		non-persistent
volatile		thread-safe	




https://github.com/jagasia/empower2025/blob/jag/Java/Java_Mock_11.docx

