JSP & JSTL
--------------
Product CRUD


oracle.jdbc.OracleDriver
oracle.jdbc.driver.OracleDriver

we will learn to convert a normal project into a maven project

action tags are always prefixed with "jsp"
<jsp:forward>
<jsp:include>
<jsp:useBean>



JSTL
------
	JSP Standard Tag Library

first we need to include this dependency in pom.xml


<dependency>
			<groupId>jakarta.servlet.jsp.jstl</groupId>
			<artifactId>jakarta.servlet.jsp.jstl-api</artifactId>
			<version>3.0.0</version>
		</dependency>

		<dependency>
			<groupId>org.glassfish.web</groupId>
			<artifactId>jakarta.servlet.jsp.jstl</artifactId>
			<version>3.0.1</version>
		</dependency>



in jsp, include this tag library using
	taglib directive

<%@taglib uri="http://java.sun.com/jsp/jstl/core" prefix="c" %>




implicit objects of JSP:
--------------------------
request
response
out
session
application
config
exception
server




TasK;

create a signup form in jsp
and on submitting the form, it should add a row to the USER_MASTER table


to get web.xml (if not already present in src/main/webapp/WEB-INF 
----------
after converting dynamic web project into maven project,
	right click the project
		-> Java EE tools	
			-> generate deployement descriptor stub



to find what is the error in pom.xml:-

right click project
	run as
		maven build
			Goals: clean install

in console, if you see BUILD SUCCESS, then no problem
otherwise, read the error message and solve