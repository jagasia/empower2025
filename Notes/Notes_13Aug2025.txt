String regex

What is Testing, Need of Testing, Types of Testings, What is J-Unit, Need of J-Unit, Programming with J-Unit

What is Testing?
	checking the performance
	identifying the bugs
	checking if it passes test cases
	check if the requirements are met
	comparing desired vs actual working
	checking the quality
	testng security features
	checking functional and non functional requirements met or not

test suite contains test cases

test case
	inputs 
	expected output

normal input
abnormal input
boundary condition



Application Under Test
Software under test
Class Under Test



Steps:
--------
create a maven project
	junit-demo-1

    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.13.2</version>
      <scope>test</scope>
    </dependency>



aut/sut

i am going to create a class that will be tested.




package com.empower.demo;

public class Mathematics {
	public int sum(int i, int j) {
		return i+2+j;
	}
	
	public int difference(int i, int j) {
		return i-2-j;
	}
	
	public int product(int i, int j) {
		return i*j;
	}
}




in src/test/java

we create the test case

right click the package inside this folder, 
	new	->		junit test case

ensure that 4 or later version i chosen

i chose 4

package com.empower.demo;

import static org.junit.Assert.*;

import org.junit.Test;

public class MathematicsTest {
	Mathematics maths=new Mathematics();

	@Test
	public void testSum() {
		int actual = maths.sum(2, 3);
		int expected=5;
		assertEquals(expected, actual);
	}

	@Test
	public void testDifference() {
		int actual = maths.difference(2, 3);
		int expected=-1;
		assertEquals(expected, actual);
	}

	@Test
	public void testProduct() {
		int actual = maths.product(2, 3);
		int expected=6;
		assertEquals(expected, actual);
	}

}




Right click the project
	run as
		junit test







To run the junit from a main method. (java code)

package com.empower.demo;

import org.junit.runner.JUnitCore;
import org.junit.runner.Result;
import org.junit.runner.notification.Failure;

public class App {

	public static void main(String[] args) {
		Result result = JUnitCore.runClasses(MathematicsTest.class);
		for(Failure failure:result.getFailures()) {
			System.out.println(failure);
		}
	}

}



life cycle methods

@BeforeClass
@Before
@AfterClass
@After

--------------------------------------------
@Ignore
	to skip this test


Introducing
	Test Suite

what is test suite?
	test suite has test cases.
	in juit, a test case is a test class.

package com.empower.demo;

import org.junit.runner.RunWith;
import org.junit.runners.Suite;
import org.junit.runners.Suite.SuiteClasses;

@RunWith(Suite.class)
@SuiteClasses({MathematicsTest1.class, MathematicsTest3.class})
public class MyTestSuite {

}


Task:
Create few test classes and then include few in test suite and run the suite.


====================
categories suite.


we will create few categories.
how?
	each category is an interface

we apply the category to each or some test methods
	a method annotated with @Test is a test method


1) create admin category

	package com.empower.demo;

public interface Admin {

}



2) create user category

package com.empower.demo;

public interface User {

}



3) mention the category for each or some of the methods

package com.empower.demo;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.fail;

import org.junit.Before;
import org.junit.BeforeClass;
import org.junit.Test;
import org.junit.experimental.categories.Category;

public class MathematicsTest1 {
	Mathematics cut=new Mathematics();
	
	@BeforeClass
	public static void setupClass() {
		
		
	}
	
	@Before
	public void setup() {
		cut.setNo1(2);
		cut.setNo2(3);
	}
	
	@Test
	@Category(Admin.class)
	public void addTest() {
		float expected=5.0f;
		cut.add();
		float actual=cut.getResult();
		assertEquals(expected, actual, 0.0f);
	}
	
	@Test
	@Category(User.class)
	public void subtractTest() {
		float expected=-1.0f;
		cut.subtract();
		float actual=cut.getResult();
		assertEquals(expected, actual,0.0f);
	}
	
	@Test
	@Category(User.class)
	public void productTest() {
		float expected=6.0f;
		cut.multiply();
		float actual=cut.getResult();
		assertEquals(expected, actual,0.0f);
	}
	
	@Test
	@Category(Admin.class)
	public void divisonTest() {
		float expected=(float)2/3;
		System.out.println(expected);
		cut.divide();
		float actual=cut.getResult();
		assertEquals(expected, actual,0.0f);
	}
}



do this for all test classes.


now, create categories suite

5) package com.empower.demo;

import org.junit.experimental.categories.Categories;
import org.junit.experimental.categories.Categories.IncludeCategory;
import org.junit.runner.RunWith;
import org.junit.runners.Suite;
import org.junit.runners.Suite.SuiteClasses;

@RunWith(Categories.class)
@SuiteClasses({MathematicsTest1.class, MathematicsTest3.class, MathematicsTest2.class})
@IncludeCategory({Admin.class})
public class MyTestSuite {

}



run the suite.

Task:

create category suite and run those test methods of a particular
category or categories




package com.empower.demo;

import org.junit.experimental.categories.Categories;
import org.junit.experimental.categories.Categories.IncludeCategory;
import org.junit.runner.RunWith;
import org.junit.runners.Suite;
import org.junit.runners.Suite.SuiteClasses;

@RunWith(Categories.class)
@SuiteClasses({MathematicsTest1.class, MathematicsTest3.class, MathematicsTest2.class})
@IncludeCategory({Admin.class, User.class})
public class MyTestSuite {

}





======================
Parameterized test

we will use one test class to run several test cases 
	using array?



our CUT:

package com.empower.demo;

public class Mathematics {
	int no1;
	int no2;
	int result;
	public int getNo1() {
		return no1;
	}
	public void setNo1(int no1) {
		this.no1 = no1;
	}
	public int getNo2() {
		return no2;
	}
	public void setNo2(int no2) {
		this.no2 = no2;
	}
	public int getResult() {
		return result;
	}
	public void setResult(int result) {
		this.result = result;
	}
	
	public void add() {
		result=no1+no2;
	}
	public void subtract() {
		result=no1-no2-2;
	}
	public void multiply() {
		result=no1*no2+2;
	}

}




OUR TEST CLASS:


package com.empower.demo;

import static org.junit.Assert.assertEquals;

import java.util.Arrays;
import java.util.Collection;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.junit.runners.Parameterized;

@RunWith(Parameterized.class)
public class MathematicsTest {
	int no1;
	int no2;
	int expectedResult;
	Mathematics maths = new Mathematics();

	public MathematicsTest(int no1, int no2, int expectedResult) {
		super();
		this.no1 = no1;
		this.no2 = no2;
		this.expectedResult = expectedResult;
	}

	@Test
	public void addTest() {
		maths.setNo1(no1);
		maths.setNo2(no2);
		maths.add();
		int actualResult = maths.getResult();
		assertEquals(expectedResult, actualResult);
	}

	@Parameterized.Parameters
	public static Collection populateValues() {
		return Arrays
				.asList(new Object[][] { { 2, 3, 5 }, { 5, 4, 9 }, { 11, 12, 23 }, { 100, 200, 300 }, { 18, 28, 46 } });

	}
}




Run this test

=====================


Handson tasks:

https://github.com/jagasia/empower2025/blob/jag/Java/Java%20Hands%20on%20-13Aug2025_set1.docx






-------------------------------------------------
-------------------------------------------------
Spring framework
--------------------
java bean
	is a re-usable java code
	it is a class that can be used in any type of project
	now a days POJO


generic services
application specific services



security
transaction management
scalability
persistence



postConstruct
prePassivate
postActivate
preDestroy



getBean()	to get an instance of my bean

what is bean?
	my class, that is marked as "bean" using 
		xml configuration		or
		annotation


x=getBean();
y=getBean();

by default, spring provides "singleton" instance of bean
we can change that to "prototype"

what is singleton?
	
package com.empower.demo;

public class Singleton {
	private static Singleton singleton=null;
	private Singleton() {
		
	}
	
	public static Singleton getInstance() {
		if(singleton==null) {
			singleton=new Singleton();
		}
		return singleton;
	}
}


