Collections in java
-----------------------
what are the limitations of array?
	fixed length		(correct)
	same data type or type	(not a limitation. every collection also)
	insertion and deletion may require shifting elements
	array elements are stored in contiguous memory locations
	slow compared to others (collections)


	

Object arr[]=new Object[10];
arr[0]=new Integer(20);		//
arr[1]="Raja";
arr[2]=new Date();


Integer i=(Integer)arr[0];



Now, you are conviced to learn collections chapter to overcome the limitations of arrays.

Collections
------------
java provides lot of collections classes, interfaces, they are called as collection API.
they belong to java.util package

Categories of collections:
List
Set

List and Set are interfaces that are derived from Collection interface

Map is an application of Collection
	Key is set
	Value is collection

Difference between List and Set
------------------------------------

List
	is ordered (means, the order in which elements are added are remembered)
	Order does not mean, asc or desc order

	allows DUPLICATES
	allows SORTING the elements (after they are added)
	Collections.sort() works only for List
	ListIterator works only for List	

	has index. we can directly access elements using their index

	ArrayList
	LinkedList
	Vector
	Stack


Set
	reject duplicates
	does not allow sorting the elements. (once they are added, their position changing is not possible)
	Set actually uses some algorithm to decide where to place an element.
	do not have index




	HashSet
		the order of elements are decided by
			hashCode() and equals() method
	TreeSet
	LinkedHashSet





Demo:
	ArrayList


import java.util.ArrayList;
import java.util.Collections;
import java.util.List;

public class App {

	public static void main(String[] args) {
		List<Integer> marks = new ArrayList<>();
		marks.add(90);
		marks.add(10);
		marks.add(10);
		marks.add(10);
		marks.add(20);
		marks.add(10);
		marks.add(30);
		
		
//		System.out.println(marks.size()); 		//output is 7
		
		Collections.sort(marks);
		System.out.println(marks);
	}

}



how to iterate collections?
	iterate means, visiting each element once
	
we can use
	for each loop for all collections	(not good idea)
		will give ConcurrentModificationException if you remove elements inside 			for-each loop

	for loop with index		only for List

	iterator
		is the best way to iterate any collection
		we can remove current element during iteration

there are 2 types of iterators
	Iterator
	ListIterator		(works only for List)


Iterator		has only 4 methods
	hasNext()		returns true if there is any more element to traverse
	next()			returns the next element
	remove()			removes the current element
	forEachRemaining


ListIterator
	hasNext
	next
	remove
	hasPrevious
	previous


	ListIterator is used to traverse in both directions


	
import java.util.List;
import java.util.ListIterator;

public class App2 {

	public static void main(String[] args) {
		List<Integer> list=List.of(11,2,5,3,9,12,7,8,10);

		ListIterator<Integer> it = list.listIterator(list.size());
		
		while(it.hasNext()) {
			Integer current = it.next();
			System.out.println(current);
		}
		System.out.println("............");
		
		while(it.hasPrevious()) {
			Integer x = it.previous();
			System.out.println(x);
		}
	}

}



--------------------
Task:
List
	create an array list of String (countries) and add countries
	and display them

to display
	try using
		for loop with index
		for each loop
		iterator
		list iterator

Observe that the output is in same order of which they are added.

and duplicates are also added.



for loop with index

	list.get(index)		return element at index 




List<Integer> list=new ArrayList<>();
list.add(1);
list.add(2);
list.add(3);

list.remove(1);		//what will be removed?	2 is removed
list.remove(new Integer(1)); //what will be removed?	1 is removed




remove(index)
remove(object)



===================
Here’s a **complete Java "Collections - List" chapter** covering:

1. **Conceptual content** with subtopics
2. **Hands-on practice tasks (without solutions)**

   * Exercises target key concepts: `ArrayList`, `LinkedList`, indexing, iteration, sorting, searching, etc.
   * Ideal for corporate trainees or students to practice independently.

---

## ✅ Java Collections: **List Interface**

### 📘 1. Introduction to List Interface

* The `List` interface is part of the Java Collections Framework.
* It allows **ordered** collection of elements (maintains insertion order).
* Allows **duplicates**.
* Elements can be accessed via **index**.

```java
List<String> names = new ArrayList<>();
names.add("Alice");
names.add("Bob");
System.out.println(names.get(0)); // Output: Alice
```

---

### 📘 2. Implementations of List

| Implementation | Characteristics                                  |
| -------------- | ------------------------------------------------ |
| `ArrayList`    | Fast random access, slow insert/delete in middle |
| `LinkedList`   | Good for frequent insert/delete, slower access   |
| `Vector`       | Synchronized, thread-safe, legacy class          |

---

### 📘 3. Common List Methods

| Method                 | Description                  |
| ---------------------- | ---------------------------- |
| `add(element)`         | Adds to end                  |
| `add(index, element)`  | Adds at index                |
| `remove(index)`        | Removes by index             |
| `remove(Object)`       | Removes first occurrence     |
| `get(index)`           | Get element                  |
| `set(index, element)`  | Replace element              |
| `contains(element)`    | Checks if exists             |
| `indexOf(element)`     | Returns index of first match |
| `lastIndexOf(element)` | Returns index of last match  |
| `size()`               | Number of elements           |
| `clear()`              | Removes all elements         |
| `isEmpty()`            | Checks if list is empty      |

---

### 📘 4. Iterating a List

```java
for (int i = 0; i < list.size(); i++) {
    System.out.println(list.get(i));
}

for (String name : list) {
    System.out.println(name);
}

list.forEach(System.out::println);

Iterator<String> it = list.iterator();
while(it.hasNext()) {
    System.out.println(it.next());
}
```

---

### 📘 5. Sorting and Searching

* `Collections.sort(list)`
* `Collections.reverse(list)`
* `Collections.binarySearch(list, element)` – **list must be sorted**

---

### 📘 6. Converting List to Array and Vice Versa

```java
List<String> list = Arrays.asList("A", "B", "C");
String[] array = list.toArray(new String[0]);
List<String> newList = Arrays.asList(array);
```

---

### 📘 7. List vs Set vs Map (Quick Compare)

| Feature         | List       | Set            | Map                         |
| --------------- | ---------- | -------------- | --------------------------- |
| Duplicates      | Allowed    | Not allowed    | Keys not allowed duplicates |
| Order           | Maintained | Not guaranteed | Key-value pair              |
| Access by index | Yes        | No             | No                          |

---

## 🛠️ Hands-on Tasks (Without Solutions)

These tasks are for independent coding practice. They gradually increase in complexity and cover key aspects of `List`.

---

### 🧪 Task 1: Basic List Operations

**Objective**: Create an `ArrayList` of strings and perform the following:

* Add 5 names.
* Insert a name at index 2.
* Remove a name by value.
* Print the size of the list.
* Display all names using a loop.

---

Solution:
-----------

import java.util.ArrayList;
import java.util.List;

public class Task1 {

	public static void main(String[] args) {
		List<String> list=new ArrayList<>();
		list.add("Sachin");
		list.add("Arjun");
		list.add("Jadeja");
		list.add("Robin");
		list.add("Kapil");
		
		list.remove("Jadeja");
		
		System.out.println(list.size());
		
		for(int i=0;i<list.size();i++) {
			System.out.println(list.get(i));
		}
	}

}


### 🧪 Task 2: Integer List Analysis

**Objective**: Create a `List<Integer>` to store 10 numbers.

* Print the even numbers.
* Find and print the sum of all numbers.
* Find the max and min number.
* Replace all odd numbers with zero.


Solution:
----------
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

public class Task2 {

	public static void main(String[] args) {
		List<Integer> list=Arrays.asList(15,42,33,42,15,96,78,78,96,50);
		int sum=0;
		int max=Integer.MIN_VALUE;
		int min=Integer.MAX_VALUE;
		System.out.println("Printing all even numbers:");
		for(Integer x:list)
		{
			sum+=x;
			if(x>max) {
				max=x;
			}
			if(x<min) {
				min=x;
			}
			if(x%2==0) {
				System.out.println(x);
			}else
			{
				x=0;
			}
		}
		
		System.out.println("Sum of all numbers:"+sum);
		System.out.printf("Max is %d and min is %d",max,min);
		
	}

}


---

### 🧪 Task 3: Search and Indexing

**Objective**:

* Ask the user to input 5 city names.
* Search if “Chennai” exists in the list.
* If yes, print its index.
* Else, print “City not found”.


import java.util.ArrayList;
import java.util.List;
import java.util.Scanner;

public class Task3 {

	public static void main(String[] args) {
		Scanner sc=new Scanner(System.in);
		System.out.println("Enter 5 city names:");
		List<String> cities=new ArrayList<>();
		for(int i=0;i<5;i++) {
			String city=sc.nextLine();
			cities.add(city);
		}
		int index=cities.indexOf("Chennai");
		
		System.out.println((index!=-1)?index:"City not found");
	}

}

---

### 🧪 Task 4: Sorting Student Marks

**Objective**:

* Create a `List<Integer>` with student marks.
* Sort the marks in ascending and descending order.
* Remove the lowest mark.
* Add a new mark at the second position.


import java.util.Arrays;
import java.util.Collections;
import java.util.List;

public class Task4 {

	public static void main(String[] args) {
		List<Integer> marks=Arrays.asList(80,75,68,94,64,59,50);
		Collections.sort(marks);		//sort to asc
		Collections.reverse(marks);		//reverse to get desc
		//remove the lowest mark. lowest is the last element
		marks.remove(marks.size()-1);		//removing last element
		marks.add(1, 100);		//add at 2nd position
		
	}

}



---

### 🧪 Task 5: Custom Object List

**Objective**:

* Create a `Student` class with fields: `id`, `name`, `marks`.
* Store 3 `Student` objects in a `List<Student>`.
* Iterate using `for` loop and `Iterator`.
* Print the student with the highest marks.

---

### 🧪 Task 6: Using LinkedList

**Objective**:

* Create a `LinkedList<String>` of programming languages.
* Add languages at the beginning, end, and middle.
* Remove the first and last element.
* Convert it to `ArrayList` and print all elements.

---

### 🧪 Task 7: Sorting Custom Objects (Try this after morning break)

**Objective**:

* Use `Comparator` or `Comparable` to sort a list of `Employee` objects by name and salary.

```java
class Employee {
   int id;
   String name;
   double salary;
}
```

---

### 🧪 Task 8: Remove Duplicates from List

**Objective**:

* Input: `["Apple", "Banana", "Apple", "Orange", "Banana"]`
* Output: List with duplicates removed but order preserved.

---

### 🧪 Task 9: Filter List using Stream API

**Objective**:

* Given a `List<String>` of names, print only the names that start with `S` and have more than 5 characters.

---

### 🧪 Task 10: Convert List to Map		(Do this after Map chapter)

**Objective**:

* Create a list of `Product` objects with `id`, `name`, `price`.
* Convert it into a `Map<Integer, String>` where key is `id`, and value is `name`.

---

After break:

Convert List to Set and vice versa
	constructors of List and Set provide this option


import java.util.ArrayList;
import java.util.Arrays;
import java.util.LinkedHashSet;
import java.util.List;
import java.util.Set;

public class App3 {

	public static List<Integer> removeDuplicates(List<Integer> input) {
		//write your code here
		//convert list into set
		Set<Integer> set=new LinkedHashSet<>(input);
		//convert set into list. then return that list
		List<Integer> result=new ArrayList<>(set);
		return result;
	}
	public static void main(String[] args) {
		List<Integer> list=Arrays.asList(91,28,73,46,54,54,35,62);
		List<Integer> result = removeDuplicates(list);
		System.out.println(result);
	}

}




Comparable and Comparator
equals method

-----------------------------
Comparable and Comparator
	are useful
		Collections.sort()
		TreeSet




Understood. From now on, all hands-on tasks will strictly stick to the requested topic and avoid referencing **future or unrelated topics** such as Map unless specifically asked.

---

Here’s your **hands-on exercises** focused **only on `Comparable`** (for use with `Collections.sort`) and **`TreeSet`**, with **no solutions** and **no Map concepts involved**.

---

## 🛠️ Hands-On: `Comparable` and `Collections.sort()`

### 📌 Goal: Understand how to sort custom objects using `Comparable` interface.

---

### 🧪 Task 1: Sort Custom Class Using `Comparable`

**Objective**:

* Create a class `Book` with fields: `int id`, `String title`, `double price`.
* Implement `Comparable<Book>` to sort books by price in ascending order.
* Create a `List<Book>` with 5 sample books.
* Sort the list using `Collections.sort()` and print the sorted result.


import java.util.ArrayList;
import java.util.Collections;
import java.util.List;

public class Task1 {

	public static void main(String[] args) {
		List<Book> books=new ArrayList<>();
		books.add(new Book(14, "You can win", 105.0));
		books.add(new Book(5, "Tale of two cities", 80.0));
		books.add(new Book(1, "Wings of Fire", 115.0));
		books.add(new Book(2, "Let us C", 100.0));
		books.add(new Book(4, "Java Complete Reference", 125.0));
		
		
		Collections.sort(books);
		
		for(Book b:books) {
			System.out.println(b);
		}
	}

}


---

### 🧪 Task 2: Sort Custom Class by Name Alphabetically

**Objective**:

* Create a class `Person` with `String name`, `int age`.
* Implement `Comparable<Person>` to sort by `name` alphabetically.
* Create a list of 5 persons and display the sorted names.

import java.util.ArrayList;
import java.util.Collections;
import java.util.List;

public class Task2 {

	public static void main(String[] args) {
		List<Person> persons=new ArrayList<>();
		persons.add(new Person("Sudheep", 40));
		persons.add(new Person("Yash", 32));
		persons.add(new Person("Raj Kumar", 100));
		persons.add(new Person("Puneeth", 39));
		persons.add(new Person("Shiva Raj", 42));
		
		Collections.sort(persons);
		
		for(Person p:persons) {
			System.out.println(p);
		}
	}

}

---

### 🧪 Task 3: Sort List of Integers with Collections.sort

**Objective**:

* Create a `List<Integer>` with 10 values.
* Sort them in ascending order using `Collections.sort()`.
* Then reverse the list using `Collections.reverse()`.

import java.util.Arrays;
import java.util.Collections;
import java.util.List;

public class Task3 {

	public static void main(String[] args) {
		List<Integer> values=Arrays.asList(130,114,125,163,417,258,196,720,281,229);
		Collections.sort(values);
		Collections.reverse(values);
		
		for(Integer x:values) {
			System.out.println(x);
		}
	}

}



---

### 🧪 Task 4: Sort List of Strings

**Objective**:

* Create a `List<String>` of country names.
* Sort alphabetically using `Collections.sort()`.
* Check if the sorting is case-insensitive.

import java.util.Arrays;
import java.util.Collections;
import java.util.List;

public class Task4 {

	public static void main(String[] args) {
		List<String> countries=Arrays.asList("India", "Australia", "Sri lanka", "Pakistan", "Bangladesh", "Japan", "Nepal", "Bhutan", "China", "Russia");
		Collections.sort(countries);
		Collections.reverse(countries);
		for(String c:countries) {
			System.out.println(c);
		}
	}

}


---

---

## 🛠️ Hands-On: `TreeSet`

### 📌 Goal: Understand how `TreeSet` maintains sorted order and does not allow duplicates.

---

### 🧪 Task 5: TreeSet with Integers

**Objective**:

* Create a `TreeSet<Integer>` and add 10 integers in random order.
* Print all elements and observe the order.
* Try adding a duplicate element and note the result.


import java.util.TreeSet;

public class Task5 {

	public static void main(String[] args) {
		TreeSet<Integer> tree=new TreeSet<>();
		tree.add(62);
		tree.add(36);
		tree.add(610);
		tree.add(62);
		tree.add(56);
		tree.add(16);
		tree.add(62);
		tree.add(76);
		tree.add(62);
		tree.add(96);

		for(Integer x:tree) {
			System.out.println(x);
		}
		
	}

}


---

### 🧪 Task 6: TreeSet with Strings

**Objective**:

* Create a `TreeSet<String>` and add city names.
* Try adding duplicates.
* Print the contents of the set to verify sorting.

import java.util.Set;
import java.util.TreeSet;

public class Task6 {

	public static void main(String[] args) {
		Set<String> cities=new TreeSet<>();
		
		cities.add("Kochi");
		cities.add("Agra");
		cities.add("New Delhi");
		cities.add("Agra");
		cities.add("Thiruvananthapuram");
		cities.add("Kolkata");
		
		for(String s : cities) {
			System.out.println(s);
		}
	}

}

---

### 🧪 Task 7: TreeSet with Custom Class Using Comparable

**Objective**:

* Create a class `Student` with fields: `int rollNo`, `String name`.
* Implement `Comparable<Student>` to sort by `rollNo`.
* Add multiple students to a `TreeSet<Student>` and print the contents.

---

### 🧪 Task 8: TreeSet with Natural and Reverse Order
**Objective**:
* Create a `TreeSet<Integer>` to store random numbers.
* Use default order first.
* Then use a constructor to provide a reverse order using `Collections.reverseOrder()` or `Comparator`.

hint:
	Collections.sort(tree,Collections.reverseOrder());
---

After lunch:
=============
Comparator
Map
Generics
Lambda
Method Reference
Stream API


Challenge:
------------
Java Mock 11	Requirement 5

Hint:
use Comparable and Comparator







College class (before implementing Comparable):-

import java.util.Date;

public class College {
	private String name, website, mobile, founder, location;
	private Integer numberOfDept;
	private Date startingDate;
	
	public College() {}

	public College(String name, String website, String mobile, String founder, String location, Integer numberOfDept,
			Date startingDate) {
		super();
		this.name = name;
		this.website = website;
		this.mobile = mobile;
		this.founder = founder;
		this.location = location;
		this.numberOfDept = numberOfDept;
		this.startingDate = startingDate;
	}

	public String getName() {
		return name;
	}

	public void setName(String name) {
		this.name = name;
	}

	public String getWebsite() {
		return website;
	}

	public void setWebsite(String website) {
		this.website = website;
	}

	public String getMobile() {
		return mobile;
	}

	public void setMobile(String mobile) {
		this.mobile = mobile;
	}

	public String getFounder() {
		return founder;
	}

	public void setFounder(String founder) {
		this.founder = founder;
	}

	public String getLocation() {
		return location;
	}

	public void setLocation(String location) {
		this.location = location;
	}

	public Integer getNumberOfDept() {
		return numberOfDept;
	}

	public void setNumberOfDept(Integer numberOfDept) {
		this.numberOfDept = numberOfDept;
	}

	public Date getStartingDate() {
		return startingDate;
	}

	public void setStartingDate(Date startingDate) {
		this.startingDate = startingDate;
	}

	@Override
	public String toString() {
		return "College [name=" + name + ", website=" + website + ", mobile=" + mobile + ", founder=" + founder
				+ ", location=" + location + ", numberOfDept=" + numberOfDept + ", startingDate=" + startingDate + "]";
	}
	
	
}









the createCollege method:-


	static College createCollege(String detail) throws ParseException {
		College college=null;
		SimpleDateFormat sdf=new SimpleDateFormat("dd-MM-yyyy");
		String[] arr = detail.split(",");
//		IIT Guwahati,www.iitg.ac.in/,9876543214,Govt of India,10,Guwahati,17-05-1994
		college.setName(arr[0]);
		college.setWebsite(arr[1]);
		college.setMobile(arr[2]);
		college.setFounder(arr[3]);
		college.setNumberOfDept(Integer.valueOf(arr[4]));
		college.setLocation(arr[5]);
		college.setStartingDate(sdf.parse(arr[6]));
		return college;
	}






import java.text.ParseException;
import java.text.SimpleDateFormat;
import java.util.ArrayList;
import java.util.List;
import java.util.Scanner;

public class App {

	public static void main(String[] args) throws ParseException {
		Scanner sc=new Scanner(System.in);
		System.out.println("Enter the number of the colleges:");
		int noOfColleges=sc.nextInt();
		List<College> colleges=new ArrayList<>();
		for(int i=0;i<noOfColleges;i++) {
			String detail=sc.nextLine();
			if(detail.equals(""))
				detail=sc.nextLine();
			College college=College.createCollege(detail);
			colleges.add(college);
		}
		
		System.out.println("Enter a type to sort:\r\n"
				+ "1.Sort by name\r\n"
				+ "2.Sort by number of departments\r\n"
				+ "3.Sort by starting date");
		
		int choice=sc.nextInt();
		
		switch(choice) {
		case 1:		//sort by name
			break;
		case 2:		//sort by number of departments
			break;
		case 3:		//sort by starting date
			break;
		}
		
		SimpleDateFormat sdf=new SimpleDateFormat("dd-MM-yyyy");

		
		System.out.format("%-15s %-20s %-15s %-15s %-20s %-15s %s\n", "Name","Website","Mobile","Founder","No of departments","Location","Starting date");
		for(College c:colleges) {
			System.out.format("%-15s %-20s %-15s %-15s %-20s %-15s %s\n", c.getName(),c.getWebsite(),c.getMobile(),c.getFounder(),c.getNumberOfDept(),c.getLocation(),sdf.format(c.getStartingDate()));
		}
	}

}





Comparator
Map
Generics
Lambda
Method Reference
Stream API



Challenge:
----------

Accept a String input from the user and display each character and its count.

Sample input:
this is an input entered by the user

Sample output:
t	4
h	2
i	3
s	3
 	7
a	1
n	3
p	1
u	2
e	5
r	2
d	1
b	1
y	1



HashMap			keys are HashSet
TreeMap			keys are TreeSet
LinkedHashMap		keys are LinkedHashSet


Map is key value pair

Keys	are Set
Values	are Collection



Entry		Key and Value


put(k,v)		add an entry to the map
		same method to
			add a new entry
			update existing entry
				if key is already found, then its value is updated/ replaced
get(k)		returns value



How to iterate a map?
--------------------------
Map do not have iterator.
But Set (keys are set) has iterator


map.keySet();		to get all keys as a Set and iterate that set
map.values();		values is collection and you can iterate them
map.entrySet();		returns a Set of Entry




import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Map.Entry;
import java.util.Scanner;

public class App {

	public static void main(String[] args) {
		Scanner sc=new Scanner(System.in);
		Map<Character, Integer> map=new LinkedHashMap<>();
		String input=sc.nextLine();
		for(int i=0;i<input.length();i++) {
			Character c=input.charAt(i);
			//if this character is already found or not?
				//found? update the value
				//not found? add new entry
			
//			Integer count = map.get(c);
//			if(count==null) {
//				count=0;
//			}
//			count++;
//			map.put(c, count);
			map.put(c, map.getOrDefault(c, 0)+1);
		}
		
		//print the output
		for(Entry<Character, Integer> entry:map.entrySet())
		{
			System.out.println(entry.getKey()+"\t"+entry.getValue());
		}
	}

}




















import java.util.Iterator;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Map.Entry;
import java.util.Scanner;
import java.util.Set;

public class App {

	public static void main(String[] args) {
		Scanner sc=new Scanner(System.in);
		Map<Character, Integer> map=new LinkedHashMap<>();
		String input=sc.nextLine();
		for(int i=0;i<input.length();i++) {
			Character c=input.charAt(i);
			//if this character is already found or not?
				//found? update the value
				//not found? add new entry
			
//			Integer count = map.get(c);
//			if(count==null) {
//				count=0;
//			}
//			count++;
//			map.put(c, count);
			map.put(c, map.getOrDefault(c, 0)+1);
		}
		
//		//print the output
//		for(Entry<Character, Integer> entry:map.entrySet())
//		{
//			System.out.println(entry.getKey()+"\t"+entry.getValue());
//		}
		
		Set<Character> keys = map.keySet();
//		for(Character key:keys) {
//			System.out.println(key+"\t"+map.get(key));
//		}
		
		Iterator<Character> it = keys.iterator();
		
		while(it.hasNext()) {
			Character key = it.next();
			System.out.println(key+"\t"+map.get(key));
		}
		
	}

}


