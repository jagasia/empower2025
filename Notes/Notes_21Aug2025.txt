Spring Boot(RESTFul Service, POSTMAN, …), Mockito, Maven Tool

zero xml configuration
convention over configuration
in-built server like apache tomcat
auto configuration
starter dependencies
starter project




demo:


https://start.spring.io/




Problem:

8080 already in use.


Solution1:
in application.properties
	server.port=8081

If problem persists:
	
in application.properties:
	debug=true

right click project
	run as
		maven build
			goals:	clean install
				run

now go to application main method
	run as java application

you will see additional debug info in console.


Steps for MVC demo 1 is sent to you all by harinath via teams chat.

Kindly follow the steps.

IMPORTANT:
	your application java file is in base package.
	that package name is base for all other classes.
		sub package name is ok

check port number

check url 		Get mapping

check if all dependencies added to pom.xml

IMPORTANT
	your system restricts downloading jars.
		so maven build clean install only help downloading jars

		right click project
			run as
				maven build
					goals:	clean install




TasK
------
create a form that accepts 2 numbers
and on submit, it should add them and display the result.

hint:

<form action="/add">

create a controller method with @GetMapping("/add")



1) create a spring boot project
	com.empower.demo
	maths-mvc-demo

	java
	maven
	java 21
	war

	spring web

2) in pom.xml

		<dependency>
			<groupId>org.apache.tomcat.embed</groupId>
			<artifactId>tomcat-embed-jasper</artifactId>
		</dependency>

3) in application.properties

server.port=8081
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp


4) in src/main/webapp
	create folder	WEB-INF/views


5) create JSP file inside views folder

<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
</head>
<body>
<form action="/add">
Number 1:<input type="number" name="no1" /><br/>
Number 2:<input type="number" name="no2" /><br/>
<input type="submit" value="Add" />
</form>
<br/>
${sum }
</body>
</html>
6) create the controller

package com.empower.demo.controller;

import org.springframework.stereotype.Controller;
import org.springframework.ui.ModelMap;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestParam;
import org.springframework.web.bind.annotation.ResponseBody;

@Controller
public class MathsController {

	@GetMapping
	public String home() {
		return "index";
	}
	
	@GetMapping("/add")
//	@ResponseBody
	public String add(@RequestParam Integer no1,@RequestParam Integer no2, ModelMap model) {
		Integer sum=no1+no2;
		model.addAttribute("sum",sum);
		return "index";
	}
}


Task:
------
Create a login page
if username and password are same, then display login successful in a new output.
if else, display login failure in red color in login page itself.




2.5 days plan for spring boot:

1) Spring boot MVC with spring jdbc product dao
JPA
2) Spring REST
3) Spring REST CRUD with JPA
	POSTMAN
		POST, GET, PUT, DELETE
4) Swagger
5) Spring security
6) Microservices
	Spring cloud



Demo:
spring jdbc in spring boot.

refer:

https://github.com/jagasia/empower2025/tree/jag/springboot/product-mvc-jdbc


@GetMapping("/hi")
@ResponseBody
public String method1(){
	return "index";
}


@RestController	=	@Controller + @ResponseBody


RESTful webservices / REST API
------------------
Steps for spring boot rest

1) create a springboot app

	group:	com.empower.task
	artifact:	rest-1
	package:	same as group
	jar
	maven
	java
	21

	spring web

2) server.port=5000		in application.properties

3) create the rest controller

package com.empower.demo.controller;

import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
public class MyController {

	@GetMapping
	public String home() {
		return "Hello world";
	}
}


4) run the main method as java application


5) http://localhost:5000/

-----------------------------------------------
Observations:

problem1:
	8080 already in use

solution:
	1) in application.properties
		server.port=8081

	2) right click project -> Run as -> Maven Build
		goals:	clean install

	there are 2 manen build options.
		1st will repeat previously executed goals
		2nd will always ask for goals

	3) now, you see build success?
		no? there will be reason for failure. Address that issue
		yes? now run the application as Java Application (dont click arrow that repeats goals)

problem2:
----------
	your Application.java is present in	
		com.example.demo

	your controller is in
		com.empower.demo.controller


ensure all classes present in
	base package		(package that contains application)
	or
	its sub package

otherwise,
	create a config class and use @ComponentScan(basePackages={})
	or in APplication class (it is @SpringBootApplication is also a configuration class only)


===============================
REST principles:
-------------------
use conventions properly
	HTTP Methods
		GET		for returning data or information
		POST		for adding a new resource
		PUT		for updating 
		DELETE		for deleting
		PATCH		for updating partial information

uniform interface:

	http://localhost:5000/product





@RequestParam			?id=20
vs
@PathVariable			/20




http://localhost:5000/product?id=20
	the above is using	Request Param

http://localhost:5000/product/20
	the above is using Path Variable



Hoppscotch




package com.empower.demo.controller;

import org.springframework.web.bind.annotation.DeleteMapping;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.PutMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RestController;

@RestController
public class MyController {

	@GetMapping
	public String home() {
		return "Hello world";
	}
	
	@PostMapping
	public String addProduct(@RequestBody Product product) {
		return "Adding product";
	}
	
	@PutMapping("/{id}")
	public String updateProduct(@PathVariable("id")Long id) {
		return "Updating product";
	}
	
	
	@DeleteMapping("/{id}")
	public String removeProduct(@PathVariable("id") Long id, @RequestBody Product product) {
		return "Product is removed";
	}
}













package com.empower.demo.controller;

import org.springframework.web.bind.annotation.DeleteMapping;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.PutMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
public class MyController {

	@GetMapping
	public String home() {
		return "Hello world";
	}
	
	@GetMapping("/{id}/{name}/{category}")
	public String kethanMethod(@PathVariable Long id, @PathVariable String category, @PathVariable String name) {
		return ""+id+":"+category+":"+name;
	}
	
	@PostMapping
	public String addProduct() {
		return "Adding product";
	}
	
	@PutMapping("/{id}")
	public String updateProduct(@PathVariable("id")Long id) {
		return "Updating product";
	}
	
	
	@DeleteMapping("/{id}")
	public String removeProduct(@PathVariable("id") Long id) {
		return "Product is removed";
	}
}




JSON		JavaScript Object Notation
	we are representing an object in javascript format

what is the difference between java script object and JSON?

javascript object:
	{id:1,name:'Scale',category:'Stationery',price:20}

JSON:
	{"id":"1","name":"Scale","category":"Stationery","price":"20"}















package com.empower.demo.controller;

import org.springframework.web.bind.annotation.DeleteMapping;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.PutMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RestController;

import com.empower.demo.entity.Product;

@RestController
@RequestMapping("/product")
public class ProductController {

	@GetMapping
	public Product getProduct() {
		return new Product(1L, "Pen", "Stationery", 12.2);
	}
	
//	@PostMapping
	@RequestMapping(method = RequestMethod.POST, consumes = "application/json")
	public Product addProduct(@RequestBody Product product) {
		return product;
	}
	
	@PutMapping("/{id}")
	public Product addProduct(@PathVariable("id")Long id, @RequestBody Product product) {
		return new Product(id, "Pen", "Stationery", 12.2);
	}
	
	@DeleteMapping("/{id}")
	public Product addProduct(@PathVariable("id")Long id) {
		return new Product(id, "Pen", "Stationery", 12.2);
	}
	
}




{
    "id":1,
    "name":"Sharpner",
    "category":"Stationery",
    "price":20
}