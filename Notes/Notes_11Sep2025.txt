docker compose.

we are going to run 2 services.
1) oracle 
	gvenzl/oracle-xe:21-slim

2) our spring boot app
	book-service-oracle

to run these 2 docker images together,
lets use
	docker compose up

the above command will look for a file:
	docker-compose.yml

YAML	=	YAML A'nt Markup Language




I want to run the following images together using docker compose yml.
1) oracle 
	gvenzl/oracle-xe:21-slim

2) our spring boot app
	book-service-oracle:latest

now, give me detailed steps including what i should update in application.properties.

here is my application.properties:
spring.application.name=book-service

server.port=5000
spring.application.name=book-service

spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.initialization-fail-timeout=60000

spring.sql.init.continue-on-error=true
spring.sql.init.mode=always


server.port=5000

# ===============================
# DataSource Configuration
# ===============================
spring.datasource.url=jdbc:oracle:thin:@oracledb:1521/t3
spring.datasource.username=system
spring.datasource.password=password
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# ===============================
# JPA / Hibernate Configuration
# ===============================
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update





docker-compose.yml:
------------------------
services:
  oracledb:
    image: gvenzl/oracle-xe:21-slim
    container_name: oracledb
    environment:
      - ORACLE_PASSWORD=password
    ports:
      - "11521:1521"
      - "5500:5500"
    healthcheck:
      test: ["CMD-SHELL", "echo 'SELECT 1 FROM dual;' | sqlplus -s system/password@//localhost:1521/XE || exit 1"]
      interval: 20s
      timeout: 10s
      retries: 10
      start_period: 40s

  book-service:
    image: book-service-oracle:latest
    container_name: book-service
    depends_on:
      oracledb:
        condition: service_healthy
    ports:
      - "5000:5000"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:oracle:thin:@oracledb:1521/XE
      - SPRING_DATASOURCE_USERNAME=system
      - SPRING_DATASOURCE_PASSWORD=password
      - SPRING_DATASOURCE_DRIVER_CLASS_NAME=oracle.jdbc.OracleDriver




in cmd:where the docker-compose.yml is present:

	docker compose up -d


check:
	docker ps -a

when both services are up,

check logs

	docker logs book-service

if you see the spring boot started,
then check swagger.

-------------------------------------------------
AWS
-----
Amazon Web Services

https://aws.amazon.com/

login to 
	AWS Management console

EC2
----
	Elastic Compute Cloud

storage
compute
network

launch a windows server ec2 instance

1) search EC2
2) instances running is 0

click "Launch instance"

3) name:		rupesh-windows
4) choose the OS	windows
5) instance type:	t3.2xlarge
6) key-value pair:	create it. it will in downloads folder	rupesh-key.pem
7) Network settings:
	edit
		Auto-assign public ip	enable

Launch instance button
------------------------------------
--------------------------------
Now we will create a linux EC2 instance.

1) in aws.amazon.com

search for EC2

2) click "Launch instance"

3) choose OS
	Amazon linux

4) t3 2x large

5) create a key pair

6) edit the n/w setting
	enable the "auto assign public ip"

7) launch instance
--------------------------------

Lets connect to our EC2 instance

1) select the instance
	and click the connect button

2) choose "ssh client" tab

copy the ssh command and paste it in cmd prompt in a location where your pem file present (downloads)

3) 






http://
3.109.206.232
:5000/book



